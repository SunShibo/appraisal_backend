<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.wisewin.api.dao.TopSearchDAO">

    <select id="getTopSearch" resultType="com.wisewin.api.entity.bo.TopSearchBO">
        select * from ap_top_search where state='yes' order by sort desc
    </select>

</mapper>